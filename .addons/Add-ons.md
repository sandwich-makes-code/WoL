# Addons
Addons will be stored here, you can make one with the pre-existing definitions or build something completely new!

Addons will be stored as completely separate versions of WoL.

## Building
You can either build with a fresh version of WoL or build with a stripped down version that contains all the variables with no interface.
